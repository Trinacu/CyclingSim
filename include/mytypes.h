#ifndef MYTYPES_H
#define MYTYPES_H

// Semantic, scenario-level identity (UI / plots / configs)
using RiderId = int;

using GroupId = int;

inline GroupId kNoGroup = -1;

using TeamId = int;

inline TeamId kNoTeam = -1;

#endif
